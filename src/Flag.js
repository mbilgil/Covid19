import React , {useEffect,useState} from 'react';

const api = {
    base: "https://restcountries.eu/rest/v2/name/"
}

const Flag = ({data}) => {

    
return (
    <img alt="" src={flag.flag} width="120" height="80" className="d-inline-block align-top" />
);

};

export default Flag;